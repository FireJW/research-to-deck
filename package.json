{
  "name": "research-to-deck",
  "version": "0.1.0",
  "description": "Public-safe research-to-deck demo that turns grounded Markdown into a deck contract, HTML preview, QC report, and run result.",
  "type": "module",
  "private": false,
  "scripts": {
    "generate": "node scripts/generate-demo.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
